# Week 10 - MySQL Practice

## 📌 Overview
This week is focused on practicing **MySQL database concepts** and strengthening query‑writing skills.  
The exercises cover table creation, constraints, joins, and aggregate functions — all essential for building reliable, real‑world databases and preparing for integration with Python automation.

## 🛠️ Topics Covered
- **Database & Table Creation**
  - Creating databases and tables
  - Applying primary keys and foreign keys
- **SQL Queries**
  - Basic operations: `SELECT`, `INSERT`, `UPDATE`, `DELETE`
  - Filtering with `WHERE`, `LIKE`, `IN`, `BETWEEN`
- **Joins**
  - Inner Join, Left Join, Right Join, Cross Join
- **Aggregate Functions**
  - `COUNT()`, `SUM()`, `AVG()`, `MIN()`, `MAX()`
  - Grouping with `GROUP BY` and filtering with `HAVING`
- **Constraints**
  - `NOT NULL`, `UNIQUE`, `CHECK`, `DEFAULT`

## 📖 Important Points
- Joins are crucial for combining data across multiple tables.
- Aggregate functions simplify reporting and analytics tasks.
- Constraints enforce **data integrity** and prevent invalid entries.
- Practicing queries improves confidence in handling **real‑world databases**.
- A strong understanding of SQL is vital before integrating with **Python automation**.

---
✨ *Week 10 builds a solid foundation for database‑driven automation projects.*
